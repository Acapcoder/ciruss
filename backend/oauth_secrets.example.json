{
  "_comment": "Copy this file to oauth_secrets.json and fill in the client_secret values. These stay on the backend and are never sent to the browser. Register {BACKEND_URL}/api/oauth/<provider>/callback as the redirect URI in each OAuth app (e.g. http://localhost:8000/api/oauth/gdrive/callback).",
  "gdrive": {
    "client_id": "YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com",
    "client_secret": "PASTE_GOOGLE_CLIENT_SECRET_HERE"
  },
  "onedrive": {
    "client_id": "",
    "client_secret": ""
  },
  "dropbox": {
    "client_id": "",
    "client_secret": ""
  }
}
